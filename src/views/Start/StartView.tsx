import React, { FC } from 'react';
import { IStartViewProps } from './StartView.models';

export const StartView: FC<IStartViewProps> = (props: IStartViewProps) => {
    return <div>Start Game View</div>;
};
